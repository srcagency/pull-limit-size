{
	"name": "pull-limit-size",
	"description": "Throw an error after `x` bytes/length",
	"version": "1.0.0",
	"repository": "srcagency/pull-limit-size",
	"scripts": {
		"test": "covert test.js"
	},
	"devDependencies": {
		"covert": "^1.1.0",
		"pull-stream": "^3.6.0",
		"tape": "^4.6.3"
	},
	"license": "MIT"
}
